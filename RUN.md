# 运行说明（RUN.md）

WG课程表 —— 项目在本地怎么跑起来、怎么验证。

本文件随开发进度更新。**当前进度：页面骨架（对应 `PRD.md` 第 7 节 T1）。**

---

## 一、项目性质

纯静态网页：只有 HTML / CSS，**没有后端、没有构建步骤、不需要安装任何依赖**。
不存在 `npm install`、不存在打包、不存在环境变量。

## 二、环境要求

| 项目 | 要求 |
| --- | --- |
| 浏览器 | 任意现代浏览器（Chrome / Edge 均可） |
| 本地服务器 | Python 3 自带的 `http.server` 模块（用来自起一个静态服务，不需要 `pip install` 任何东西） |

> **为什么不能直接双击 `index.html`？**
> 双击打开时地址栏是 `file://` 开头。浏览器的部分能力（本地存储、后续要加的 Service Worker）
> 在 `file://` 下会被限制或行为不一致。用 `http://localhost` 打开，与将来部署到
> GitHub Pages 后的环境更接近。且 `file://` 打开时**不满足** PRD 验收里对地址栏的要求。

## 三、本机 Python 路径（重要，这台机器的特有坑）

**本机 cmd 里直接输入 `python` 是无效的**，执行后只会静默退出、没有任何提示。

原因：`where python` 指向

```
C:\Users\Administrator\AppData\Local\Microsoft\WindowsApps\python.exe
```

这是 Windows 的「应用执行别名」占位程序（微软商店的空壳），不是真正的 Python。

**本机可用的 Python 完整路径：**

```
C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python.exe
```

> 提示：这个 Python 属于工具自带的受管版本，路径里带版本号（`3.13.12`），
> 将来若升级可能变化。若该路径失效，用 `where python` 或到
> `C:\Users\Administrator\.workbuddy\binaries\python\versions\` 下查看当前版本目录。

## 四、本地运行（三步）

**第 1 步：打开命令行**

按 `Win + R`，输入 `cmd`，回车。（**不需要管理员权限**）

**第 2 步：进入项目目录**

```cmd
cd /d D:\WG课程表
```

> `/d` 的作用是「允许跨盘符切换目录」。少了它，从 C 盘切到 D 盘会失败。

**第 3 步：启动静态服务器**

```cmd
C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python.exe -m http.server 8001
```

**启动成功的标志** —— 出现下面这一行，然后光标停住不动：

```
Serving HTTP on :: port 8001 (http://[::]:8001/) ...
```

**光标停住是正常的**：它在等待访问请求，不是在卡死。**这个窗口在停止服务前不要关闭。**

**停止服务**：在命令行窗口按 `Ctrl + C`。

## 五、访问与验证

浏览器地址栏输入：

```
http://localhost:8001
```

打开后应看到（对照检查）：

1. **地址栏**以 `localhost` 开头；
2. **页头**居中显示「WG课程表」；
3. **页面中央**一张白色卡片，写着「还没有课表数据」+ 一行灰色说明 + 蓝色按钮「去导入课表」；
4. **页面背景**是浅灰色，不是纯白（说明 `css/base.css` 加载成功）；
5. **页面底部**有固定导航条，三个入口：今日 / 导入 / 设置，当前页高亮为蓝色；
6. **点击底部导航**可在三个页面之间跳转。

### 验证时的两个注意点

- **窗口太扁时看不到底部导航。** 底部导航固定在**视口底部**，若浏览器窗口高度过小（约 400px 以下），
  它会被挤出可视区域。把窗口拉高或按 `F11` 全屏即可看到。这是正常行为，不是 bug。
- **样式完全没生效（白底黑字）怎么办。** 说明服务器把错误的目录当成了根目录。
  检查第 2 步是否真的执行了 `cd /d D:\WG课程表`。服务器以「执行启动命令时所在的目录」
  作为网站根目录，目录不对就取不到 `css/` 下的文件。按 `F12` 打开开发者工具，
  Console 面板会看到资源 `404` 报错。

## 六、常见问题排查

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 输入 `python ...` 后无任何输出，直接回到提示符 | 本机 `python` 是商店空壳别名 | 改用第三节的完整路径 |
| `This site can't be reached` / 无法访问此网站 | 服务器没在运行，或命令未在项目目录下执行 | 确认命令行窗口里有 `Serving HTTP on ...` |
| `OSError: [WinError 10048] address already in use` | 端口被占用（常见于上一次的服务器未关闭） | 换一个端口，如 `8002` |
| 页面能打开但样式全丢 | 启动服务器时的目录不对 | 见第五节第二个注意点 |
| 改动代码后刷新看不到变化 | 浏览器缓存 | 按 `Ctrl + F5` 强制刷新 |

## 七、当前进度与后续

| 状态 | 内容 | 对应任务 |
| --- | --- | --- |
| ✅ 已完成 | 页面骨架：三个页面、页面间跳转、首页空状态视图 | T1 |
| ⬜ 未完成 | 课表文件解析 | T2 |
| ⬜ 未完成 | 导入页三个视图（预览 / 成功 / 失败） | T3 |
| ⬜ 未完成 | 数据本机存储 + 课程增改删 | T4 |
| ⬜ 未完成 | 今日视图（周数、今日课程、状态标签） | T5 |
| ⬜ 未完成 | 节次时间表 → 状态与「下一节课」 | T6 |
| ⬜ 未完成 | 周视图网格 | T7 |
| ⬜ 未完成 | 设置页（起始日期、节次时间、数据管理） | T8 |
| ⬜ 未完成 | PWA 配置（清单 / 图标 / 全屏）与公网部署 | T9 |
| ⬜ 未完成 | `.ics` 日历导出 | T10 |
| ⬜ 未完成 | 整体验收 | T11 |

> 任务编号与 `PRD.md` 第 7 节一致。

## 八、当前页面骨架的已知边界

以下均为**有意为之**，不是缺陷：

1. **三个页面目前不包含任何 JavaScript。** 页面跳转靠普通链接完成，因此即使禁用 JS 也能正常跳转。
2. **导入页与设置页的按钮全部处于禁用状态。** 它们的功能分别属于 T3 / T4 / T8，本阶段只摆位置，
   避免出现「看起来能用、实际是半成品」的界面。
3. **首页的空状态为静态显示。** 尚未接入「读取本机是否有数据」的判断逻辑（属 T4）。
4. **尚无 PWA 能力。** 没有 `manifest.json`、没有 Service Worker、没有图标文件，
   因此暂时无法「添加到手机主屏幕」，断网也还不能用。属 T9。

---

*本文件创建于训练营 Day 7，对应 `PRD.md` 第 7 节 T1 的「运行命令存档」要求。*
