# WG课程表 · PRD（产品需求文档）

| 项目 | 手机端实时课表（面向大学生） |
| --- | --- |
| 文档 | `PRD.md` |
| 日期 | 2026-09-19（训练营 Day 4）；**2026-09-23 修订（Day 8）** |
| 版本 | **v1.1**（修订内容见第 11 节变更记录） |
| 上游文档 | `research.md`（Day 3 需求研究）—— 功能范围与数据格式均以其为依据 |
| 下游 | Day 5 技术选型（本文不涉及任何技术选型与代码） |
| 产品形态 | **C 方案：先做网页（PWA，可添加到手机主屏幕），小程序作为后续可选外壳** |

## 0. 一句话定位

给大学生用的手机网页课表：点一下桌面图标，**马上看到「今天第几节、上什么课、在哪上、开始了没」**——没有广告，不用注册，不用下载 App。

## 1. 要解决的三个问题（本 PRD 的靶心）

| # | 问题 | 现状 | 本产品的答案 |
| --- | --- | --- | --- |
| P-1 | 看课表要先进教务系统 | 登录 → 找入口 → 等加载 → 自己心算第几周 | 添加到主屏幕，点开图标直接见课表，周数自动算 |
| P-2 | 现有课表 App 广告多、模块杂 | 社区、推广、成绩等模块稀释核心体验 | 只有一张课表：零广告、零社交、零推广模块 |
| P-3 | 用户要的是「下一节课去哪」 | 整学期大表，需要人脑解读 | 首页直接标出「正在上课」的一节和「下一节课」 |

## 2. 目标用户

- **主要**：在校大学生（本科为主）。每天 2–6 节课，课程带周次差异（单双周、起止周、隔周上），课表每学期变一次。
- **行为特征**：手机不离手；不愿为一个课表下载 App 或注册账号；把课表当隐私，不想上传服务器。
- **验证条件**：本人即目标用户（安卓手机），除 PWA 真机行为外，全部验收可真机自测。
- **本期不覆盖**：其他学校教务系统的导出格式（只保证本人学校那份文件能解析）；iPhone / iOS 路径（记为后续）。

## 3. MVP 功能清单（8 项，均为最小可用）

| # | 功能 | 落在哪个页面 | 一句话规格 | 范围边界（本期不做） |
| --- | --- | --- | --- | --- |
| F1 | 导入课表文件 | 导入页 | 选择教务系统导出的 `.htm` 文件，解析出全部课程；解析结果**先预览、确认后才替换**已有数据 | 不做教务一键导入、不做 OCR（见第 6 节） |
| F2 | 手动录入 / 编辑课程 | 导入页 | 不依赖教务文件也能用：课程名、星期、节次、地点、教师、周次集合，支持增、改、删 | 不做批量编辑、不做课程复制 |
| F3 | 周视图 | 首页（切换） | 周一至周日 × 每天 12 节的整周网格；当前周、今天有高亮 | 不做学期整学期总览 |
| F4 | 今日视图（首页默认） | 首页 | 当前周数 + 今天日期 + 今日课程列表；每节课带状态：已结束 / 正在上课 / 未开始；顶部固定显示「下一节课」信息条；读取本机数据期间显示「加载中」（V1.7），读取失败显示明确说明与「重试」（V1.8） | 不做课前推送提醒 |
| F5 | 学期设置 | 设置页 | ① 学期第一周周一的日期（算周数的基准，默认 2026-08-31，可改）② 节次时间表（第 1–12 节各自的起止时间，**手动录入**，默认给一版常见作息作初始值、可逐节改——没有它「开始了没」算不出来） | 不做多学期管理 |
| F6 | 数据存本机 + 备份 | 设置页 | 数据保存在浏览器本地；提供「导出备份 / 导入备份」按钮，换手机不丢 | 不做账号体系、不做云同步（见第 6 节） |
| F7 | 手机适配 + 添加到主屏幕 | 全局 | 移动端优先布局；配置 PWA（图标、名称、全屏声明），部署到公网可访问 | 不做应用商店上架、不做小程序发布（见第 6 节） |
| F8 | 导出课表日历（.ics） | 设置页 | 把整学期课程按周次展开成具体日期，生成一个 `.ics` 文件供下载；导入手机系统日历后，每节课成为日历事件（课程名 + 时间 + 地点）——白拿系统日历的桌面小组件和课前提醒，不需要服务端 | 不做日历自动同步：教务改课 → 重新导入课表 → 重新导出 .ics |

## 4. 页面与视图列表

页面共 3 个。每个页面按「状态」拆成若干视图，开发与验收逐个对照。

### P1 首页（今日课表，打开产品的默认落点）

| 视图 | 什么时候显示 | 显示什么 |
| --- | --- | --- |
| V1.1 今日有课（默认） | 已有课表数据，且今天有课 | ① 顶部信息条：第 N 周 · 今天日期 · 当前时间；② **「下一节课」信息条**：下一节的课程名、节次、地点、开始时间；今天全部课已结束时，此条显示「今天的课都上完了」；③ 今日课程列表（按节次从早到晚排列），每条含：课程名、节次、上课地点、教师、状态标签（已结束 / 正在上课 / 未开始），「正在上课」的一条高亮 |
| V1.2 今日没课 | 已有课表数据，今天没课 | 周数 + 日期 +「今天没有课」的明确提示 |
| V1.3 非教学周 | 当前日期早于学期第一天，或晚于学期最后一天 | 「还没开学 / 已放假」提示，不显示课程 |
| V1.4 本周没课 | 已有课表数据中，本周没有课 | 周数 + 日期 +「本周没有课」的提示 |
| V1.5 空课表 | 从未导入过课表，一条课程都没有 | 空状态引导：「先去导入课表」按钮，点击跳到导入页 |
| V1.6 周视图 | 用户在首页切换到周视图 | 整周网格（周一至周日 × 12 节，行=节次、列=星期）；当前周数、今天列高亮；**只显示本周有课的格子**（周次集合含本周）；每格显示课程名 + 地点（放不下则截断 + 省略号） |
| V1.7 加载中 | 打开首页后正在读取本机数据，**且读取耗时超过 250ms** | 「正在读取课表…」+ 加载指示。读取很快（不足 250ms）时**不显示**本视图——一闪而过的加载页比不显示更糟 |
| V1.8 读取失败 | 读取本机数据抛错，或读到的数据无法识别（结构不对、版本高于当前程序） | 「没能读到本机保存的课表」+ 一句原因说明 + **「重试」按钮**（重新读取一次）。**不得显示成空课表**（V1.5 的含义是「从未导入过」，两者含义不同，混用会让用户以为课表丢了），也不得白屏 |

**两条补充规则（避免开发各理解一套）**：

1. **V1.7 / V1.8 是首页级的过程状态**：出现时今日视图与周视图的内容都不显示；恢复后回到原本该显示的视图（判断规则见 V1.1–V1.5）。
2. **「浏览器禁用了本地存储」（无痕模式等）不属于 V1.8**。该情况按 `TECH_DESIGN.md` 6.3 降级处理——仍允许本次会话内查看并明确提示，只有「读得到却读不成」才走 V1.8。

### P2 导入页

| 视图 | 什么时候显示 | 显示什么 |
| --- | --- | --- |
| V2.1 导入前 | 还没选择文件 | 「选择课表文件」按钮 + 三步操作说明（教务系统 → 学生个人课表 → 列表展示 → 导出）；「手动添加课程」入口 |
| V2.2 导入成功 | 文件解析成功 | 解析结果统计（共 N 条课程记录）+「保存并使用」按钮；**保存前先预览**，保存时若已有课表数据，需二次确认「将替换现有课表」 |
| V2.3 导入失败 | 文件解析失败 | 明确报错并**区分原因**：① 不是可识别的课表文件 ② 解析到 0 条课程 ③ 文件为空；引导重试 |
| V2.4 课程管理 | 点开课程列表 | 已有课程列表；可新增、修改、删除单条课程（字段见 5.1） |

### P3 设置页

| 视图 | 什么时候显示 | 显示什么 |
| --- | --- | --- |
| V3.1 常规设置 | 常规 | 学期第一周周一的日期（日期选择）；节次时间表（12 行，每行开始/结束时间，支持手动逐节录入与修改） |
| V3.2 数据管理 | 常规 | 「导出备份」「导入备份」「导出课表日历（.ics）」「清空全部数据（需二次确认）」四个按钮 |

## 5. 数据字段

### 5.1 课程数据（核心字段）

| 字段 | 说明 | 示例 | 页面显示 |
| --- | --- | --- | --- |
| 课程名 | 必填 | 复变函数与积分变换 | 首页、周视图 |
| 星期 | 1–7（1 = 周一），用于筛选「今天」 | 2 | 周视图列头（不逐条显示） |
| 节次 | 第几节（两位一段的数字串，`0102` = 第 1–2 节；解析时按 01–12 两位一段切分） | 第 6–7 节 | 首页、周视图 |
| 上课地点 | **允许为空**（真实数据里体育课地点为空，显示「—」） | 教三-302 | 首页、周视图 |
| 教师 | 允许为空（为空时该位置留空，不显示占位符） | 张老师 | 首页、周视图（可省略） |
| 周次 | **周次集合**，不是「起止周+单双周」（真实数据存在 1,5,9,13 和 11–16 这类不规则周次）；仅同课程名 + 同星期 + 同节次 + 同地点的记录可合并，合并后取并集 | 1,5,9,13 | 不直接显示，用于筛选「本周/今天」 |

**两条数据规则（文档必须定死，不能留给开发猜）**：

1. **合并规则**：跨周次拆成多条时，同课程名 + 同星期 + 同节次 + 同地点 → 周次取并集合并为一条；地点不同的记录**不合并**（避免把「同一门课换教室」错误合并掉）。
2. **冲突兜底**：若同一时间段（同星期 + 同节次 + 同周次）出现两门不同课程，本期**不做冲突提示、不做自动裁决**，两条都照常显示——线上课与线下课同时占用该时段是真实情况（数据来自教务，正常不会冲突）。

解析时额外保留（本期不显示、不参与功能）：班级名称、排课日期、课序、类型。

### 5.2 设置数据

| 字段 | 说明 |
| --- | --- |
| 学期第一周周一的日期 | 周数计算的基准，默认 2026-08-31（本人学校本学期），可修改 |
| 节次时间表 | 第 1–12 节各自的开始/结束时间，**手动录入**（默认给一版常见作息作初始值，可逐节改）；「正在上课 / 未开始 / 已结束」与「下一节课」都由它算出 |

### 5.3 解析目标（只定结果，不定实现）

导入 `.htm` 后必须得到：上面 5.1 的 6 个核心字段，**逐次课记录**（一门课每周上几次就是几条记录，天然支持单双周与不规则周次），并按 5.1 的两条数据规则合并。格式细节（两位切分规则、12 列表头）见 `research.md` 第四节，此处不重复。

## 6. 本期不做（后续规划）——砍了什么，为什么砍

> 今日主题的正面回答。以下每项都已明确砍掉，理由与替代方案逐条写明，防止中途被诱惑。
> **计数说明**：下表共 11 行，其中「导出 .ics」已由用户拍板转为做的（保留在此仅作范围变化留痕），实际「不做」为 10 项。

| 砍掉的功能 | 为什么砍 | 替代方案 / 重启条件 |
| --- | --- | --- |
| 教务系统一键导入（登录自动抓取） | 小程序受平台限制不能访问教务网站；各校接口不同且随时改版，维护成本远超 28 天 | 用户自己从教务导出文件，产品只负责解析（F1），准确率 100% |
| 拍照 / 截图识别课表（OCR） | 浏览器端中文表格 OCR 模型大且慢；云端识别要密钥、服务端、花钱，截图含隐私 | 解析教务导出的文本文件，绕开识别问题 |
| 导出 `.ics` 到系统日历 | ~~原本在「不做」清单里~~ → **已改为本期 MVP（F8）**，2026-09-19 用户拍板：能白拿系统日历的桌面小组件与课前提醒，价值大于其额外开发量。此行保留仅作范围变化留痕 | —（已在第 3 节 F8 落地） |
| 应用商店上架 / 小程序发布 | 个人主体发布需 ICP 备案 + 认证，周期以周计且不可控 | 先交付网页形态（加主屏幕已达「点一下就看到」），小程序留作后续外壳 |
| 自己做桌面小组件 | 网页与小程序都拿不到系统级小组件 | 导出 .ics（F8，已纳入本期）→ 用系统日历自带小组件 |
| 账号体系、云同步、多设备 | 引入服务端、登录与隐私责任，本期只做单机 | 本地备份文件（F6）承担「换手机不丢」 |
| 社交功能（找同学、蹭课、树洞） | 超级课程表的教训：稀释核心体验，需要审核运营 | 不做 |
| 成绩查询、空教室、考试倒计时 | 与「此刻该去哪」无关，功能膨胀 | 不做 |
| 主题商城 / 完整装饰系统 | 本期只做最简配色区分课程 | 后续按需 |
| 调课 / 临时变动 | 真实痛点，但明显增加数据模型复杂度 | MVP 跑通后评估；过渡办法：重新导出导入 |
| 课前推送提醒（自建） | 需要服务端与推送资质 | 导出 .ics（F8）导入系统日历后，系统自带提醒，零成本达成 |

## 7. 开发任务拆解（每个任务 ≤ 1 天，供 Day 5 之后排期用）

| # | 任务 | 内容 | 前置 |
| --- | --- | --- | --- |
| T1 | 页面骨架 | 三个页面 + 页面间跳转 + 首页空状态视图（V1.5） | — |
| T2 | 文件解析 | `.htm` → 逐次课记录（含两位切分、周次集合、按 5.1 两条规则合并），用真实导出文件验证 | T1 |
| T3 | 导入页 UI | V2.1 / V2.2 / V2.3 三个视图（预览 + 替换前二次确认） | T2 |
| T4 | 数据存储 + 课程管理 | 本机保存；课程增改删（V2.4）；**读取失败时向上抛出可识别的错误**（供 V1.8 显示，不吞异常、不返回空数组冒充「没有课」） | T2 |
| T5 | 今日视图 | 周数计算、今日课程过滤、字段显示（V1.1 / V1.2 / V1.3 / V1.4）；**读取过程状态：加载中（V1.7）与读取失败含重试（V1.8）** | T4 |
| T6 | 状态与下一节 | 节次时间表 → 状态标签 + 「下一节课」信息条 | T5 |
| T7 | 周视图 | 整周网格 + 高亮（V1.6） | T4 |
| T8 | 设置页 | 起始日期、节次时间表、备份导出/导入/清空（V3.1 / V3.2） | T4 |
| T9 | PWA 与部署 | 图标、全屏、公网访问、真机「添加到主屏幕」验证 | T5 |
| T10 | .ics 导出 | 课程 + 周次集合 + 起止日期 → 生成日历文件；处理不连续周次；设置页加导出按钮（V3.2） | T9 |
| T11 | 整体验收 | 逐条过第 8 节验收清单，修复问题 | 全部 |

> 说明：任务编号 = 建议的推进顺序，前置列表示「做完哪些才能开始」。T3 / T4 / T5 / T7 / T8 之间存在可并行的空间，但按编号顺序做最稳。每个任务 ≤ 1 天，11 个任务约 2 周（含验收与修补余量），留足 28 天中的缓冲。

## 8. 验收标准（可逐条检查）

> 全部通过 = MVP 完成。每条都写明「怎么查、预期看到什么」。
> 「真实文件」= 本校教务导出的 `学生个人课表-列表展示导出.htm`（不入仓库）。

**A. 导入**
- [ ] A1 用真实文件导入：解析成功，显示记录条数，无报错。
- [ ] A2 解析结果抽对 3 条：课程名、节次、地点、教师、周次与文件原内容一致（其中一条节次形如 `01020304` 的连堂课）。
- [ ] A3 故意选一个非课表文件（如随手一张图片或其他 `.htm`）：显示明确报错，不崩溃、不写入脏数据。
- [ ] A4 抽一条地点为空的课（如体育课）：地点位置显示「—」，不是空白、不是「undefined」。
- [ ] A5 抽一条周次不连续的课（如 1,5,9,13 或 11–16）：周次与文件原内容一致，未被错误填成连续区间。
- [ ] A6 导入前已有课表数据时保存：出现「将替换现有课表」二次确认；取消则原数据不变。

**B. 首页（今日课表）**
- [ ] B1 顶部显示的周数与日期正确（对照校历/教务）。
- [ ] B2 首页列出今天的每一节课，按节次从早到晚排列，每条含课程名、节次、地点、教师。
- [ ] B3 上课时间段内打开：该课标「正在上课」且高亮；同一天里未开始的课标「未开始」、已上完的标「已结束」。
- [ ] B4 「下一节课」信息条的课程、节次、地点、开始时间与课表一致；当天课全上完后该条显示「今天的课都上完了」（无下一节时不显示空白卡片）。
- [ ] B5 把某门课的周次改成不含本周（编辑验证）：首页当天列表里它消失。
- [ ] B6 三种空状态分别可见：①今天没课 →「今天没有课」②不在教学周 → 假期提示 ③本周全无课但有课表数据 →「本周没有课」。
- [ ] B7 从未导入过数据时首页显示「先去导入课表」按钮，点击能跳到导入页。
- [ ] B8 读取耗时超过 250ms 时首页显示「正在读取课表…」；读取很快（不足 250ms）时**不出现**该视图、内容直接显示、不闪烁（验证时机：T4 接入本机存储之后）。
- [ ] B9 让本机数据变成「读得到但读不成」（用 `F12` 手工改坏本机存储里的数据结构，或写入一个当前程序不认识的版本号）：首页显示明确说明与「重试」按钮，**不能显示成「还没有课表数据」**（否则用户会以为课表丢了）、不能白屏；点「重试」能重新读取（验证时机：T4 接入本机存储之后）。

**C. 周视图**
- [ ] C1 切换到周视图：网格完整（行=第 1–12 节、列=周一至周日），当前周数正确，今天所在列高亮。
- [ ] C2 周次集合含本周的课出现在正确格子（星期与节次都对），不含本周的不出现。
- [ ] C3 一门跨多个节次的连堂课（如第 1–4 节）在周视图里跨越对应行数或明确标出范围，不与其他课重叠错位。

**D. 设置与数据**
- [ ] D1 修改学期起始日期：周数随之变化且算得对。
- [ ] D2 修改某节起止时间：状态标签与「下一节课」随之变化。
- [ ] D3 手动新增一门课，能在首页/周视图看到；修改、删除同样生效。
- [ ] D4 刷新页面、关闭再打开浏览器：数据还在。
- [ ] D5 导出备份 → 清空全部数据（有二次确认）→ 导入备份：课表完整恢复。
- [ ] D6 设置页导出课表日历：`.ics` 文件成功生成并可下载。
- [ ] D7 把 `.ics` 导入手机系统日历：抽查 3 节课的事件，课程名、时间、地点正确。
- [ ] D8 周次不连续的课（如 1,5,9,13 周）：日历事件只落在这几周对应的那几天，其余日期没有。

**E. 形态体验**
- [ ] E1 手机浏览器打开 → 添加到主屏幕 → 点图标：全屏独立窗口、无地址栏。
- [ ] E2 断网状态下打开：课表照常显示（数据在本机）。
- [ ] E3 全程无广告、无注册登录、无任何推广模块。
- [ ] E4 手机竖屏下首页一屏内可看到「下一节课」信息条，无需左右滑动。

**F. 边界与隐私**
- [ ] F1 真实课表 `.htm` 文件与含真实信息的备份文件**不在 Git 仓库里**（检查 `git status` 与仓库文件列表）。
- [ ] F2 完成标准以外不多做：没有第 6 节清单里任何功能的入口、按钮或半成品（多做了就记入下一版，不塞进本期）。
- [ ] F3 全部数据只存在浏览器本机，无任何网络请求把课表内容发送到外部。

## 9. 风险与待定项

1. **教务导出格式各家不一**：本期只保证本人学校的导出文件能解析；其他学校格式列为后续适配（`research.md` 第九节第 11 条）。
2. **PWA 真机行为未实测**：安卓 Chrome「添加到主屏幕」是否生成全屏应用，取决于 PWA 配置，T9 阶段真机验证。
3. **`.ics` 的日历兼容性**：各手机 / 日历 App 对 .ics 的导入行为可能有差异（事件样式、重复规则的表现），本期只真机验证本人安卓手机 + 系统日历这条主路径。
4. **快照失效**：教务改课后需重新导出导入，产品内加一句提示即可，不做自动同步。

## 10. 本期暂缓但刻意保留的设计（后续可选）

- **用颜色区分不同课程**：第 6 节把「主题商城 / 装饰系统」列为不做，但**课程配色**属基础可用性（用户在颜色上认课），本期留有位置；时间紧可退化为纯文字。
- **周视图左右滑动切换周次**：看未来几周安排时需要，本期 T7 有余量可加，不加不算失败。
- **本地存储被清理时的提示**：不影响主流程，无余量可不做。

## 11. 变更记录

| 版本 | 日期 | 改了什么 | 为什么 |
| --- | --- | --- | --- |
| v1.0 | 2026-09-19（Day 4） | 初版 | — |
| **v1.1** | **2026-09-23（Day 8）** | 首页新增两个视图：**V1.7 加载中**（延迟 250ms 才显示）、**V1.8 读取失败**（含「重试」按钮）；同步更新第 3 节 F4、第 7 节 T4 / T5、第 8 节新增验收 B8 / B9 | Day 8 清单要求首页含「加载中 / 成功 / 空 / 错误」四种状态，而 v1.0 只有「成功 + 四种空」（验收 F2「不多做」当时记为下一版）。本版按用户 2026-09-23 拍板补入。同时修正一处口径：**「浏览器禁用本地存储」仍走降级（`TECH_DESIGN.md` 6.3），不算 V1.8** |

**本次拍板的三个实现约束**（2026-09-23 用户选定）：

1. 加载中视图**延迟 250ms** 才显示——本机读取通常只要几十毫秒，直接显示会一闪而过；
2. 错误视图**必须带「重试」按钮**（对应 `TECH_DESIGN.md` 6.1 第 1 条「告诉用户下一步做什么」）；
3. 本期**不做产品内的状态演示开关**（不用网址参数、不在数据文件里留开关），两个状态按真实触发条件生效；
   在接入本机存储（T4）之前，只能由开发自检脚本模拟触发验证。

---

*本文为 Day 4 产出，已通过 AI 自检（自检记录：3 项「必须修改」已修完，末轮无遗留）。范围依据 `research.md`，含两处已确认的范围变化：① MVP 由 7 项增至 8 项（`.ics` 导出，2026-09-19 用户拍板纳入）；② 首页视图由 6 个增至 8 个（新增 V1.7 加载中、V1.8 读取失败，2026-09-23 用户拍板纳入，见第 11 节变更记录）。技术选型、目录结构、具体实现均不在本文范围，见 Day 5。*
